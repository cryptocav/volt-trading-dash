title: "VOLT Trading Dashboard"
description: >
  Backtest results, PineScript indicators, strategy sweep summaries, and live dev logs.

remote_theme: just-the-docs/just-the-docs
color_scheme: light

# This is the missing piece:
collections:
  docs:
    output: true
    permalink: /:path/
    # The following tells Jekyll where the collection lives:
    directory: _docs

just_the_docs:
  collections:
    docs:
      output: true
      permalink: /:path/
      directory: _docs   # ← make sure this line is present

url: "https://cryptocav.github.io"
baseurl: "/volt-trading-dash"

search_enabled: true
favicon_ico: "/assets/images/favicon.ico"
